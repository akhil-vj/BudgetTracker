# Backend Configuration Example

# JWT Secret Key (Generate a long random string: openssl rand -hex 32)
SECRET_KEY="your-super-secret-key-change-in-prod"

# Email Notifications (Optional - Resend API key)
# RESEND_API_KEY="re_..."

# Push Notifications (Optional - Private VAPID key)
# VAPID_PRIVATE_KEY="..."
